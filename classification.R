install.packages("rpart.plot")
library(rpart.plot)
library(rpart)
mydata=read.csv(file.choose())
class_tree=rpart(reported~gdp+arrivals+population+ch_pop+distance_km,data=mydata,method="class",cp=0)
rpart.plot(class_tree)
str(mydata)
printcp(class_tree)
mydata$reported=as.factor(mydata$reported)